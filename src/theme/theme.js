import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#57C5B6',
      dark: '#159895',
      contrastText: '#fff'
    },
    secondary: {
      main: '#002B5B',
      contrastText: '#fff'
    },
  },
});